{
  "name": "cybernetictask",
  "version": "1.0.0",
  "main": "handler.js",
  "scripts": {
    "deploy": "export PATH=$(npm bin):$PATH &&   serverless deploy",
    "local-invoke": "npx serverless invoke local --function validateUser --path ./event-validate.json"
  },
  "devDependencies": {
    "bcryptjs": "^2.4.3",
    "serverless": "^4.1.7",
    "serverless-iam-roles-per-function": "^3.2.0",
    "serverless-plugin-scripts": "^1.0.2",
    "speakeasy": "^2.0.0",
    "uuid": "^10.0.0"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "aws-sdk": "^2.1651.0"
  }
}
