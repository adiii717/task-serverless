{
  "name": "cybernetictask",
  "version": "1.0.0",
  "main": "handler.js",
  "scripts": {
    "deploy": "export PATH=$(npm bin):$PATH && serverless deploy",
    "local-invoke": "npx serverless invoke local --function UserLoginHandler --path ./test/validateUser.json",
    "destory": "export PATH=$(npm bin):$PATH && serverless remove "
  },
  "devDependencies": {
    "serverless": "^4.1.7",
    "serverless-associate-waf": "^1.2.1",
    "serverless-iam-roles-per-function": "^3.2.0",
    "serverless-plugin-lambda-insights": "^2.0.0",
    "serverless-plugin-scripts": "^1.0.2"
  },
  "author": "",
  "license": "ISC",
  "description": "cybernetictask performed by Adil (adilm717@gmail.com)",
  "dependencies": {
    "aws-sdk": "^2.1651.0",
    "speakeasy": "^2.0.0"
  }
}
